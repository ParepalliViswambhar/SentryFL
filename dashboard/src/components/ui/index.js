/**
 * UI primitives barrel
 *
 * Shared, theme-driven building blocks used across every page. Import from
 * `components/ui` rather than reaching into individual files.
 */

export { PageHeader, Panel, StatTile, EmptyState } from './surfaces';
export { LiveDot, StatusChip, ConnectionPill, ElapsedTime, RunProgress } from './live';
export { default as Sparkline } from './Sparkline';
